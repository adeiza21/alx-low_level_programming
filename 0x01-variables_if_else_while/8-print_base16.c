#include <stdio.h>
#include <stdlib.h>

/**
 *fdf f
 *dfdf dfas
 * d ff
 *ff df
 */

int main(void)
{
	char c;
	for (c = '0'; c <= '9'; c++)
	{
		putchar(c);
	}
	for (c = 'a'; c <= 'f'; c++)
	{
		putchar(c);
	}
	return (0);
}
