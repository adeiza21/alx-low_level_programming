i#include <stdio.h>
#include <stdlib.h>

/**
 *
 * dfdf d dd
 * f ddd dd
 */

int main(void)
{
	int n, c, s;
	for(n = 48, c = 44, s = 32; n <= 57; n++)
	{
		putchar(n);
		putchar(c);
		putchar(s);
	}
	return (0);
}
